<h1>Students</h1>

<table>
  <% @students = Student.all %>
  <% @students.each do |student| %>
    <tr>
        <th>Fname</th>
        <th>Lname</th>
        <th>Email</th>
        <th colspan="3"></th>
    </tr>
    <tr>
        <td><%= student.fname %></td>
        <td><%= student.lname %></td>
        <td><%= student.email %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "new student", student_newStd_path %>