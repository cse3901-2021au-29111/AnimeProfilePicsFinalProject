<h1>Roster List:</h1>

<table>
  <% @teams = Team.all %>
  <% @teams.each do |team| %>
    <tr>
      <th>
        <%= team.tName %>
      </th>
        <% team.students.each do |student| %>
          <td>
            |<%= student.fname %> <%= student.lname %>
          </td>
        <% end %>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "add students to teams", roster_newRost_path %>