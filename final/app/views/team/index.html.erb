<h1>Teams!</h1>

<table>
  <% @teams = Team.all %>
  <% @teams.each do |team| %>
    <tr>
      <th>Name</th>
    </tr>
    <tr>
      <td><%= team.tName %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "new team", team_new_path %>